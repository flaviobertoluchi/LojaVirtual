@inject LojaVirtual.Site.Extensions.Sessao sessao
@{
    var colaboradorToken = sessao.ObterColaboradorToken();
}

@model Paginacao<CategoriaViewModel>

@if (colaboradorToken is not null && colaboradorToken.Permissao.VisualizarCategoria)
{
    <div class="table-responsive">
        <table class="table table-hover">

            <thead>
                <tr>
                    <th scope="col">@Html.DisplayName(nameof(CategoriaViewModel.Id))</th>
                    <th scope="col">@Html.DisplayName(nameof(CategoriaViewModel.Nome))</th>
                    <th scope="col" class="text-end">
                        @if (colaboradorToken.Permissao.AdicionarCategoria)
                        {
                            <a class="btn btn-outline-primary" style="min-width: 42px;" asp-area="Administracao" asp-controller="Categoria" asp-action="Adicionar">
                                <i class="fa-solid fa-square-plus"></i>
                            </a>
                        }
                    </th>
                </tr>
            </thead>
            <tbody class="table-group-divider">

                @if (Model?.Data.Count > 0)
                {
                    foreach (var item in Model.Data)
                    {
                        <tr>
                            <th scope="row">@item.Id</th>
                            <td>@item.Nome</td>
                            <td class="text-end">
                                <a class="btn btn-outline-secondary" style="min-width: 42px;" asp-area="Administracao" asp-controller="Categoria" asp-action="Editar" asp-route-id="@item.Id">
                                    <i class="fa-solid fa-pen-to-square"></i>
                                </a>
                            </td>
                        </tr>
                    }
                }

            </tbody>
        </table>
    </div>

    <partial name="_PaginacaoPartial" />

}
