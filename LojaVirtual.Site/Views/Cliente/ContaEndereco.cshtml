@model ICollection<EnderecoViewModel>
@{
    ViewBag.Title = "- Conta - Endereços";
}

<div class="container mt-4">

    <div class="row justify-content-center">
        <div class="col p-3 rounded shadow bg-body">
            <div class="row">
                <div class="col-sm-3">
                    <partial name="_ContaPartial" />
                </div>
                <div class="col-sm-9">

                    <div class="row mb-3">


                        <div class="col-md">

                            <h4 class="mb-3">Endereços</h4>

                            <form method="post">

                                @if (ViewBag.Mensagem is not null)
                                {
                                    <div class="alert alert-danger" role="alert">@ViewBag.Mensagem</div>
                                }

                                @if (TempData["Sucesso"] is not null)
                                {
                                    <div class="alert alert-success" role="alert">@TempData["Sucesso"]</div>
                                }

                                <div class="table-responsive">
                                    <table class="table table-hover">

                                        <thead>
                                            <tr>
                                                <th scope="col">@Html.DisplayName("Endereço")</th>
                                                <th scope="col">@Html.DisplayName("Logradouro")</th>
                                                <th scope="col">@Html.DisplayName("Número")</th>
                                                <th scope="col">@Html.DisplayName("Complemento")</th>
                                                <th scope="col" class="text-end">
                                                    <a class="btn btn-outline-primary" style="min-width: 42px;" asp-area="" asp-controller="Cliente" asp-action="ContaEnderecoAdicionar">
                                                        <i class="fa-solid fa-square-plus"></i>
                                                    </a>
                                                </th>
                                            </tr>
                                        </thead>
                                        <tbody class="table-group-divider">

                                            @if (Model?.Count > 0)
                                            {
                                                foreach (var item in Model)
                                                {
                                                    <tr>
                                                        <td>@item.EnderecoNome</td>
                                                        <td>@item.Logradouro</td>
                                                        <td>@item.Numero</td>
                                                        <td>@item.Complemento</td>
                                                        <td class="text-end">
                                                            <a class="btn btn-outline-secondary" style="min-width: 42px;" asp-area="" asp-controller="Cliente" asp-action="ContaEnderecoEditar" asp-route-id="@item.Id">
                                                                <i class="fa-solid fa-pen-to-square"></i>
                                                            </a>
                                                        </td>
                                                    </tr>
                                                }
                                            }

                                        </tbody>
                                    </table>
                                </div>

                            </form>

                        </div>
                    </div>

                </div>
            </div>
        </div>
    </div>
</div>
